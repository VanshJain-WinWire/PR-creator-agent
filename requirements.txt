# Azure DevOps and Git integration
azure-devops==7.1.0b4
GitPython==3.1.40

# API and utilities
requests==2.31.0
python-dotenv==1.0.0

# Microsoft AI Foundry integration (for AI agent capabilities)
# Uses Azure OpenAI compatible API
# No additional package needed - uses standard requests library

# Teams Bot Framework (Optional - only needed for Teams bot)
# Uncomment the following lines if you want to use the Teams bot feature:
# botbuilder-core==4.15.0
# botbuilder-schema==4.15.0
# aiohttp==3.9.1

# Note: If you get build errors on Windows, you have two options:
# 1. Install Microsoft Visual C++ Build Tools from: https://visualstudio.microsoft.com/visual-cpp-build-tools/
# 2. Use Python 3.11 or 3.12 instead of Python 3.14 (better package support)
