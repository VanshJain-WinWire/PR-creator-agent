# Teams Bot Framework dependencies
# Install these ONLY if you want to use the Teams bot feature
# Usage: pip install -r requirements-teams.txt

botbuilder-core==4.15.0
botbuilder-schema==4.15.0
aiohttp==3.9.1

# Note for Windows users:
# If you get build errors (missing Visual C++ or Rust):
# 
# Option 1: Install Visual C++ Build Tools
# Download from: https://visualstudio.microsoft.com/visual-cpp-build-tools/
# During installation, select "Desktop development with C++"
#
# Option 2: Use Python 3.11 or 3.12
# Python 3.14 is very new and some packages don't have pre-built wheels yet
# Recommended: Python 3.11.x or 3.12.x
#
# Option 3: Use conda (has pre-built binaries)
# conda install -c conda-forge aiohttp
